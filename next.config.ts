const nextConfig = {
  images: {
    domains: ["alenort.com"], // Agrega tu dominio aquí
    unoptimized: true, // Deshabilita la optimización de imágenes de Next.js
  },
};

export default nextConfig;
